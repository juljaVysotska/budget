import { Col } from "react-bootstrap"
import { Layout } from "../Layout"
import { Chart } from "../../components"

export const Stats = () => {
    return (
    
        <Layout>
            <Col lg="9">
                <Chart />
            </Col>
        </Layout>)
}